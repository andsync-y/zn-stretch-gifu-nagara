---
// Microsoft Clarity（ヒートマップ・セッション録画）。
// プロジェクトIDはクライアントに露出する公開タグIDのため直書き（GA4と同じ扱い）。
// スクリプトは <Fragment set:html> で生HTML注入（Astro/esbuildにJSとして解析させないため）。
const clarityId = 'xpl6ca49qv';

const clarityHtml = `<script>
(function(c,l,a,r,i,t,y){
c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
})(window, document, "clarity", "script", "${clarityId}");
</script>`;
---
<Fragment set:html={clarityHtml} />
