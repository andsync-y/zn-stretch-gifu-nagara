---
// Meta（Facebook）ピクセル本体。
// ピクセルIDは環境変数 PUBLIC_META_PIXEL_ID から読み込む（ハードコードしない）。
// 未設定の場合は何も出力しない（ビルド・表示に影響を与えないためのガード）。
//
// スクリプトは <Fragment set:html> で生HTMLとして注入する。
// （<script> 要素として書くと Astro/esbuild がfbqスニペットをJSとしてバンドル解析し、
//   ビルドが失敗するため。set:htmlならそのままHTMLに出力される＝公式ベースコードと同挙動。）
const pixelId = import.meta.env.PUBLIC_META_PIXEL_ID;

const pixelHtml = `<script>
!function(f,b,e,v,n,t,s)
{if(f.fbq)return;n=f.fbq=function(){n.callMethod?
n.callMethod.apply(n,arguments):n.queue.push(arguments)};
if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
n.queue=[];t=b.createElement(e);t.async=!0;
t.src=v;s=b.getElementsByTagName(e)[0];
s.parentNode.insertBefore(t,s)}(window,document,'script',
'https://connect.facebook.net/en_US/fbevents.js');
fbq('init','${pixelId}');
fbq('track','PageView');
</script>
<noscript><img height="1" width="1" style="display:none" src="https://www.facebook.com/tr?id=${pixelId}&ev=PageView&noscript=1"/></noscript>`;
---
{pixelId && <Fragment set:html={pixelHtml} />}
