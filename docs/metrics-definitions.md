# 指標定義書（数字を結論に使う前に必ず読む）

2026-08-22 作成。**この対話で5件の誤りを出した反省から作った。**
原因はすべて同じ「その数字が何を数えているかを確認せずに結論に使った」。

## 鉄則

1. **新しいフィールドを使う前に `get_fields` で定義を読む。** 名前から推測しない
2. **単位の違う数字を比較しない。** 特に「アクション数」と「実来店数」
3. **1つの指標だけで判断しない。** CPAを見るならCPCも見る（8/14の失敗はこれ）
4. **店舗システムの指標は定義をオーナーに確認する。** 「離客」で誤読した

---

## 広告：クリック系の落とし穴

| フィールド | 実際に数えているもの | 使ってよい場面 |
|---|---|---|
| facebook `clicks` | **いいね・シェア・プロフィールクリックを含む全クリック** | ほぼ無い。効率指標に使わないこと |
| facebook `actions_landing_page_view` | **LPが実際に開かれた回数** | サイト訪問1人あたりの単価はこれで出す |
| facebook `actions_offsite_conversion_fb_pixel_lead` | **広告に帰属したピクセルLead** | Metaの最適化・学習量の判断はこれ |
| google_ads `clicks` | 検索広告のクリック | サイト訪問数として使ってよい |

**過去の誤り**：`clicks` から「Meta CPC ¥32」と算出したが、実態は
LPビュー単価 ¥41〜49 だった（2026-08-22）。

## 広告：コンバージョン系の落とし穴

| フィールド | 実際に数えているもの |
|---|---|
| google_ads `conversions` | **入札が最適化している対象**。実来店ではない |
| GA4 `click_reserve` / `click_line` / `click_tel` | **サイト全体**のCTAクリック（全流入） |
| facebook `actions_..._pixel_lead` | **Meta広告経由のみ**のLead |

**GA4のCTAクリックとMetaのLeadは別物。** GA4はオーガニック・Google広告・
直接流入を全部含む。Metaの学習に使えるのは広告帰属分だけ。

**過去の誤り**：GA4の週104〜118件を「MetaのLeadが週100件超」と誤認。
実際のMeta帰属Leadは週6.3件だった（2026-08-22）。

## アクション → 実来店の換算率（実測）

| 区間 | 換算率 | 出典 |
|---|---|---|
| 予約アクション（GA4全流入） → 新規来店 | **17〜19%** | 2026-08の週次実測 |

**CPAを報告するときは、実来店あたりの単価も併記する。**
例：Google CPA ¥1,810/コンバージョン は、実来店あたり約¥10,000 に相当する。

⚠️ `zenryoku-facts` に「実来店の目安＝予約アクション×0.6〜0.7」とあるが、
**実測は0.17〜0.19。この記述は誤り**（2026-08-22 時点で未修正）。

> **2026-08-30 再検証。この17〜19%はおおむね正しい。**
> 予約通知メールから初回予約の実数を数えたところ、8月の
> 予約アクション → 初回**予約** は 15.5%（週次 12.6〜18.6%）だった。
> 来店ベースはノーショー分だけこれを下回る。
>
> ⚠️ **2026-08-28 に私が「実測0.08〜0.11」と訂正したが、その訂正のほうが誤りだった。**
> 「8月の実来店 週4〜6人」を根拠にしたが、実際の純初回予約は月80件＝週約18.6件で、
> 店舗システムの「8月 週16人」と整合する。0.08〜0.11 は使わないこと。
> 根拠は `docs/analytics/booking-email-correlation.md`。

## 店舗システムの指標

| 指標 | 定義（オーナー確認済み） |
|---|---|
| **離客** | **新規来店数 − 回数券販売(新規)本数。それだけ。**（2026-08-24に6週分すべてで一致を確認） |
| 新規離客率 | 離客の裏返しなので、**新規販売率とほぼ同じことを言っている**。単独で深刻度を判断しない |
| 新規販売率 | 回数券販売(新規)本数 ÷ 新規来店数 |
| 更新販売率 | 更新販売本数 ÷ 使い切り人数。**回数券の消化に数ヶ月かかるため、開店初期の数値は評価不能** |
| 稼働率 | 稼働(H) ÷ 営業(H)。営業(H)＝ベッド4台×営業時間 |

⚠️ **「次回予約も取らなかった人」という説明は、データと合わない**（2026-08-24 検証）。
次回予約率は26〜40%あるのに、離客数からは1人も引かれていない。6週すべてで
`離客 = 新規 − 回数券販売` がぴったり一致する。つまり**離客率は新規販売率の裏返しにすぎず、
次回予約を取った人も「離客」に数えられている。** 実態より悲観的に出るので、
単独で深刻度を語らないこと。**次回予約率と必ず並べて見る。**

例：8/17週は離客率94.7%だが、同じ週の次回予約率は40.0%（過去最高）だった。

**過去の誤り**：離客率74.1%を「新規の4分の3が消える」と解釈したが、
実際は新規販売率26%の裏返しで、KPI目標を達成していた（2026-08-22）。

## 来店経路（店舗システム）— 唯一の「広告→実来店」の接点

`by_channel`（来店経路別販売状況）に、**新規1人ずつがどの経路から来たか**が入っている。
2026-08-24まで一度も取得していなかった。**広告の良し悪しを最終的に判定できるのはここだけ。**

⚠️ 3つの注意：

1. **受付での自己申告**。厳密な計測ではない
2. **「未記入」は再来店の人**（2026-08-24 オーナー確認）。経路を聞くのは初回だけなので空欄になる。
   ダッシュボードの「新規」には含まれるが、**新規獲得ではないので、経路分析からは必ず除く**。
   2026年8月は63人中11人（17%）が未記入＝再来店。**除かずに数えると獲得の落ち込みが隠れる**
   （7月6人→8月14.8人と増えているため、除外前は−29%、除外後は−38%になる）
3. **Google広告→/lp→HPBで予約、という人は「ホットペッパービューティー」に記録される。**
   検索広告の成果がHPBに吸収されるため、この表だけで「Google広告は効いていない」と結論しない

## 稼働率の分母

| | 値 |
|---|---|
| 店舗システムの計算 | ベッド4台 × 日数 × 12h ＝ **月1,488h**（31日）。8月の稼働201h → 13.5% |
| `zenryoku-facts` の旧記述 | 月400時間 |

**3.7倍ずれている。** 2026-08-24の週次レビューは400h基準で「稼働率57%」と報告したが、
同じ月を店舗システムは13.5%としている。**稼働率を語るときは、どちらの分母かを必ず書く。**

## 監視の基準（単一指標で判断しない）

| 対象 | 見る指標 |
|---|---|
| Google広告 | CPA **かつ CPCの絶対値**（CPC ¥200×3日で通知） |
| Meta広告 | LPビュー単価 **かつ** Lead単価。`clicks`ベースの数字は報告に使わない |
| 全体 | 広告費だけでなく**総クリック数**（費用を増やして訪問が減る事故を検知する） |

**過去の誤り**：8/14にCPA基準だけで入札戦略の継続を判断。CPCが¥98→¥307に
3倍化していたのを見逃した（2026-08-22にリバート）。

---

## データの到着遅延（判断の期日をこれで決める）

| 媒体 | 遅延 | 例 |
|---|---|---|
| **Google広告** | **2日** | 8/24朝に取れるのは 8/22 まで。`google_ads_yesterday` も8/22を返す |
| Meta広告 | 1日 | 8/24朝に 8/23 まで取れる |
| GA4 | 1日 | |
| Clarity | 当日（3日ぶんの集計） | |

**2026-08-24に確認。** リレーの時刻を早めても、手動で再取得しても変わらない。Windsor側かGoogle側の
処理遅延であり、こちらでは短縮できない。

⚠️ **「変更した翌朝に効果を見る」はGoogleでは成立しない。**
変更日をD日とすると、D日のフル1日ぶんのデータが見えるのは **D+2日の朝**。
検証日を設定するときは必ず2日足すこと。

## Clarity の読み方の注意

| 指標 | 注意 |
|---|---|
| `averageScrollDepth` | **ページの全長に対する割合。** 長いページほど低く出る。ページ間の比較に使わない |
| `EngagementTime.activeTime` | ページ長に依存しない。**熱量の比較はこちらを使う** |
| `pagesPerSessionPercentage` | LPのような単独完結ページでは1.0付近が設計通り。直帰の悪さとして読まない |
| `Browser` | InstagramApp / FacebookApp が多い＝Metaのアプリ内ブラウザ。計測がやや不安定 |

## Meta フリークエンシー（広告疲労）

`frequency` = 1人あたりの平均表示回数。**週3〜5回を超えたらクリエイティブの入れ替えを検討する。**

⚠️ **日次の `frequency` や `reach` を足し算しない。** 同じ人が複数日に見た分が重複するため、
日次を合算すると実態より低く出る。期間の値が要るときは、`date` を外して期間指定で問い合わせる。

**2026-08-10〜08-23の実測**：日次は1.05〜1.21だが、14日の実値は A=1.79 / B=2.51。基準内。

## Google 表示シェアの内訳（打ち手が正反対になる）

| フィールド | 意味 | 高いときの打ち手 |
|---|---|---|
| `search_impression_share` | 出られたはずの検索のうち実際に出た割合 | — |
| `search_budget_lost_impression_share` | **予算不足**で出られなかった割合 | 増額が効く |
| `search_rank_lost_impression_share` | **広告ランク不足**で出られなかった割合 | **増額しても消化できない。** 品質スコア・入札額・LPの見直し |

**2026-08の実測**：8/18〜8/20は予算不足0%・順位不足68〜76%。この状態で増額しても1円も使えない。
8/16に/lp切替＋新RSAへ差し替えており、新しい広告は品質スコアの履歴が無いため広告ランクが下がる。時期が一致する。

`period-compare.mjs` に検知を実装済み（`GOOGLE_LOSING_TO_RANK_NOT_BUDGET`）。

## GBP（Googleビジネスプロフィール）の実績指標

2026-08-23まで**一度も取得していなかった**。ローカル検索の効果はここでしか測れない。

| フィールド | 意味 |
|---|---|
| `impressions` / `impressions_mobile_maps` / `impressions_mobile_search` | 地図・検索での露出 |
| `website_clicks` / `call_clicks` / `direction_requests` | 露出後の行動 |
| `search_keyword` / `search_keyword_value` | **どんな検索語で見つかっているか**（地図枠SEOの直接の材料） |
| `review_id` / `review_star_rating` / `review_comment` / `review_reply_comment` / `review_reviewer` | 口コミと返信状況 |

⚠️ 口コミのフィールド名を `reviewer_display_name` / `star_rating` / `comment` と誤っていたため、
**リレーは長期間エラーで新着口コミを検知できていなかった**（2026-08-23に修正）。正しくは `review_` 接頭辞つき。

## Meta 予算のフィールド

`daily_budget` / `budget_remaining` は存在しない。正しくは **`adset_daily_budget` / `adset_budget_remaining`**
（2026-08-23に修正）。これが取れていなかったため、「日¥3,000のつもりが実際¥6,000」を2週間検知できなかった。

## 予約通知メール由来の数字（`scripts/booking-count.mjs`）

**メールの通数を予約件数として数えてはいけない。** 2026-08-31 に同じ日のうちに3回間違えた。

| 落とし穴 | 正しい扱い |
|---|---|
| 件名で `予約連絡` を検索する | **「【当日◯時】直前予約が入りました」が落ちる。** 件名で絞らず `from:salonboard.com` で引く |
| キャンセル→取り直しを2件と数える | 同一人物・同一来店日の再予約は**取り直し1件**。時刻だけ変える形が実在する |
| 同じ予約番号の通知を重ねて数える | 予約番号で重複排除する |
| 月曜を土日と比べる | 曜日差が3〜5倍ある（8月実測：土19・日14・木4）。**同じ曜日と比べる** |

⚠️ **電話予約（予約番号 `YH…`）はメールが飛ばないため、1件も見えない。**
2026-08-24 の来店5件のうち2件が電話予約だった。この経路で出せるのは
**「ネット予約（`BF`）の実数」であって「予約の実数」ではない**。総数は店舗システムで確認する。

⚠️ **1日あたり数件の母数では、変化を数日では検知できない。**
初回予約は月80件＝日2.6件。50%落ちても気づくのに約2週間かかる。
「変更して数日様子を見る」は安全弁として機能しない。効果を確かめたい変更は、
件数ではなく**動作そのもの（実機で通るか）**で確認する。

### 経路アンケートの回答の在り処

⚠️ **`■ご要望・ご相談` ではない。** 2026-08-31 に「そこに載るはず」と想定して実装したが、
実際の1通目（BF60199476・15:21受付）で否定された。`■ご要望・ご相談` は `-` のままで、
回答は **`■サロンからお客様への質問` ブロック末尾の「回答：」行**に入る。

```
■サロンからお客様への質問
　質問：…当店を「最初に」知ったきっかけの番号を下記にご記入ください。
【1】ホットペッパーで検索 …
　回答： 【1】
```

`booking-count.mjs` は `surveyAnswer`（生）と `surveyChannel`（1〜6）として取り出し、
日別に経路の内訳を出す。番号以外の自由記述は `surveyChannel` に落とさない。

### 経路アンケートを読むときの2つの制約

**① 既存客の回答を分母に入れない。**
初回の1通目（BF60199476）は**回数券保有者がHPBから予約したもの**だった。
既存客の「最初に知ったきっかけ」は何年も前の出来事で、今回の来店経路ではない。
`booking-count.mjs` は初回限定クーポンの予約だけを経路の分母にし、
回数券・チケット消化の予約は `surveyFromNonFirst` として分けて出す。

| 分類 | 判定 |
|---|---|
| `first` | 初回限定クーポンを使っている |
| `repeat` | メニューが回数券・チケット消化 |
| `unknown` | どちらとも言えない（通常メニューをクーポンなしで予約した等） |

**② 【1】は他経路を吸収する。**
設問は「**最初に**知ったきっかけ」だが、それでも防ぎきれていない。
オーナー観測では **公式LINE →「予約方法」バナー → ネット予約 → HPBへ遷移** という経路が実在し、
この人はHPB上で予約するので【1】ホットペッパーで検索を選びやすい。看板・通りがかりも同様。

**したがって【1】の件数は上限値であり、「HPB経由が多い」と読んではいけない。**
広告の効果として意味があるのは **【2】Instagram・Facebookの広告 と【3】Google検索・広告 の増減**。
LINE経由を分けたければ、アンケートではなく**LINEのバナー先に別クーポンを割り当てる**のが確実
（予約通知メールの「ご利用クーポン」で判別できる）。


## 店舗システムの売上（`kpi-data:weekly_kpi.json`）

⚠️ **`sales_total` と `new_ticket_sales` は払い戻しを引いていない。**

2026-08-24週で実際にずれた。

| | 自動取得 | 店舗システムの画面（正） |
|---|---|---|
| 売上 | ¥398,540 | **¥332,540** |
| 新規販売 | 4件 | **3件** |

差は **¥66,000 ちょうど**。8/30 16:43 の払い戻し1件（回数券とみられる）が反映されていなかった。
来店38・新規22・更新2/3・指名11 は一致していたので、**ずれるのは金額と販売本数だけ**。

**払い戻しは例外的な事象なので、スクレイパーで自動的に差し引くことはしない。**
代わりに使う側が毎回確かめる。手順は2つだけ。

1. **内訳と検算する。** 施術売上＋回数券売上＋指名 が `sales_total` と一致するか。
   内訳が手元に無ければオーナーに1問だけ聞く。**一致しなければ内訳の合計が正。**
2. **差が出たら金額をそのまま探す。** 決済のレシートメールに「◯◯円 の払い戻し」として残る。
   **差額がきれいな数字なら、まず払い戻しを疑う。**

### なぜ重要か ― 1か所のずれが結論を変える

`sales_total` は下流に連鎖する。

```
週商 → 4週平均 → 月換算 → 分岐点までの距離 → 「あと何本売ればいいか」
```

2026-08-31 の週次レビューは「**①営業利益ゼロまであと13.5万＝週0.6本**」と出したが、
実数で引き直すと **−20.7万**（月換算 ¥1,931,355 → ¥1,859,329）。
「あと1本」と「あと1.5本」では今週やることが変わる。

成約率も同様に 18.2%（4/22）→ **13.6%（3/22）** へ変わる。

### 回数券の平均単価を推定に使わない

「回数券本数 × 平均単価」で週商を説明する計算は、実数の内訳が取れるなら使わない。
8/24週は「6本×¥51,801＝¥310,806＝週商の78%」と推定していたが、
実数は **回数券売上 ¥189,640（週商の57%）／5本／平均 ¥37,928**。
本数・単価・比率のすべてが違っていた。


## GBPの「検索面の表示回数ゼロ」は指標側の故障（2026-09-01 結論）

2026-08-24 から `impressions_mobile_search` が 0 で記録され続け、
ダッシュボードが「Googleビジネスプロフィールの露出が消えている」「6〜7日連続ゼロ」と
最重要課題として出し続けていた。**実物を見て否定された。**

| 確認 | 結果 |
|---|---|
| スマホで「全力ストレッチ 岐阜」 | **表示される**（ナレッジパネル・写真・電話・経路すべて） |
| **シークレットモード**（未ログイン） | **表示される** |
| GBPの登録名 | `全力ストレッチ岐阜長良店`（正） |
| 審査待ちの編集 | なし |
| `has_voice_of_merchant` | true（正常公開） |
| 重複リスティング | なし |

**プロフィールは正常に出ている。ゼロなのは指標のほう。**

### 判断ルール

- **`impressions_mobile_search` / `impressions_desktop_search` が 0 でも、
  `impressions_mobile_maps` と `direction_requests` が動いていれば、まず指標の故障を疑う。**
  プロフィールが本当に消えたなら、マップ面も一緒に落ちる。
- **データ側の議論で結論を出さない。** 実機で検索して出るかを見るのが唯一の確定方法。
  未ログイン（シークレット）で確認すること。管理者としてログイン中の表示は根拠にならない。
- GBPのレポートは**後から埋まる**。2026-08-22・08-23 のゼロは数日後に 70・45 へ埋まった。
  **直近2〜3日のゼロを異常として報告しない。**

### この件で私が2回見立てを変えた

1. 8/31「GA4のorganicが落ちていないから計測断の疑い」
2. 9/1「8/22・23が埋まったのに8/24以降が埋まらない。遅延では説明がつかないので実害の疑い」
3. 9/1「シークレットで表示された。指標側の故障で確定」

**数字だけで往復させた。実物を1回見れば済んだ。**
GBPの露出を疑うときは、議論の前に実機確認を先に置くこと。


### ビジネス名の巻き戻りは追うのではなく検知する

2026-08 に「GBPのビジネス名が `全力ストレッチ岐阜長良店` → `全力ストレッチ店` へ繰り返し戻る」事象があった。
2026-09-01 に「外部データに `全力ストレッチ店` の表記があるのでは」という仮説を立てて
オーナーが `全力ストレッチ店 岐阜` で検索したが、**そのような表記を使っているサイトは無かった。仮説は外れ。**

現況（2026-09-01）は名前が正・審査待ちなし・`has_voice_of_merchant` true・重複なしで、**実害は出ていない**。

**実害が出ていないものを推測で追いかけない。** 代わりに `gmb_profile_state` を日次で取得し、
次のいずれかが起きたら異常として検知する。

- `location_title` が `全力ストレッチ岐阜長良店` でない
- `location_metadata_has_pending_edits` が true
- `location_metadata_has_voice_of_merchant` が false
- `location_metadata_duplicate_location` が非null

再発したら**発生の時刻と、その前後で何が起きたか**が残る。そこから原因を追えばいい。
