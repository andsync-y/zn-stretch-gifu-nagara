# 計測ランブック（GA4 / Google広告 / Meta）

「広告 → 体験 → 成約」を1本の線にするための計測設定。
2026-08-19にWindsor経由で実データを取得し、現状を確認したうえで書いている。

---

## 1. 現状（2026-08-19 実測）

### Google広告のコンバージョン（直近28日）

| コンバージョンアクション | カテゴリ | 主要CV | すべてのCV |
|---|---|---:|---:|
| 全力ストレッチ岐阜長良店 (web) hpb_click | BOOK_APPOINTMENT | **60** | 60 |
| ローカル アクション - 経路 | GET_DIRECTIONS | 4 | 57 |
| 全力ストレッチ岐阜長良店 (web) click_tel | PHONE_CALL_LEAD | 3 | 3 |
| Calls from ads | PHONE_CALL_LEAD | 1 | 1 |
| Local actions - Website visits / Other engagements / Menu views | （副次） | 0 | 422 |

- **入札戦略はすでに `MAXIMIZE_CONVERSIONS`（コンバージョン数の最大化）**。
  提案書にあった「手動入札から切り替える」は完了済み。
- 主要CVは28日で **68件**。提案書がAI Max導入の判断ラインとしていた「月30件」を超えている。
- 直近7日：費用 40,834円 / CV 25件 → CPA 約1,633円（＝予約クリック単価）。

### GA4のイベント（直近28日）

| イベント名 | 件数 | Google広告へのインポート |
|---|---:|---|
| click_reserve | 273 | — （下の `hpb_click` 経由でインポート済み） |
| hpb_click | 265 | ✅ 主要CVとして計上 |
| click_tel | 21 | ✅ 主要CVとして計上 |
| tel_tap | 3 | — |
| click_line | 21 | ❌ **未インポート** |
| line_click | 21 | ❌ **未インポート** |

`hpb_click` / `line_click` / `tel_tap` はリポジトリのコードには存在しない。
GA4管理画面の「イベントを作成」で、サイトが送る `click_reserve` / `click_line` / `click_tel` から
派生させた名前だと考えられる（数がほぼ一致することからの推定）。

---

## 2. 残っている作業：LINEクリックをコンバージョンに入れる（施策C）

**これが施策Cで唯一残っている穴。** LINEは8/17のLP改修で予約導線の主役の一つに引き上げたのに、
Google広告の学習信号に入っていない。入れないと、Googleは「LINEで予約する人」を無視して
HPBをクリックする人だけに寄せて配信し続ける。

### 手順（2026-08-20 実施済み）

GA4側では `line_click` は**すでにキーイベント**になっていた。詰まっていたのはGoogle広告側だけ。

1. Google広告 → **目標 → 概要 → ＋新しいコンバージョンアクション**
2. データソース：**ウェブサイトで発生したコンバージョン**（GA4プロパティ 545472920 が連携済み）
3. **「リンクされたアカウントから複数のコンバージョンアクションを作成する」→ 選択**
4. `line_click` にチェック → カテゴリ **「お問い合わせ」**（= CONTACT / 画面によっては「連絡先」表記）

#### ⚠️ 手動ウィザードは保存に失敗する（2026-08-20 実測）

上記4まで進めて保存すると、**「新しいコンバージョンアクションが保存されませんでした」**で必ず失敗した。

| 試したこと | 結果 |
|---|---|
| 何度か再試行 | ✕ 毎回同じエラー |
| シークレットウィンドウ（拡張機能なし） | ✕ 同じ |
| カテゴリを「リードフォームの送信」に変更 | ✕ 同じ |

**回避策**：ウィザードの「コンバージョンの作成と測定」画面に、Google自身が出す
**「ユーザーがイベント line_click を完了するとインポートされます」という提案**が表示される。
その **「提案を承認」** ボタンから作成すると**通る**（手動保存とは別の処理経路のため）。

同じ症状が出たら、手動で粘らずに提案ボタンを探すこと。

#### カテゴリは「予約」にしない

`hpb_click` が「予約」カテゴリなので、同じにすると同一の目標グループにまとめられ、
**意図せず主要CVとして入札に効いてしまう**。「お問い合わせ／連絡先」なら別グループになる。

#### ⚠️ 作成しただけでは副次にならない（必ず作成後に確認する）

作成時のダイアログには「『連絡先（ウェブサイト）』はアカウントのデフォルトの目標ではありません」
と表示され、「サブアクション」のラジオボタンもグレーアウトして選べない。
**この表示を信用しないこと。** 実際に作られたアクションは、
**アカウントのデフォルト目標「連絡先」の中に「メイン」として**入っていた（＝入札に効く状態）。

作成後に必ず **目標 → 概要** で次を確認する。

1. 目標グループ（連絡先）の左肩に **「アカウントのデフォルト」** のラベルが付いていないか
2. 付いている場合、そのアクションの「アクションの最適化」列が **メイン** になっていないか

メインだった場合の直し方（2026-08-20 に実施済み・これで副次になった）:

1. コンバージョンアクション名のリンクを開く
2. **設定を編集** → **アクションの最適化**
3. **「入札単価の最適化に使わないサブアクション」** を選択して保存

※作成時はグレーアウトしていても、**作成後の編集画面では選べる**。

目標グループ自体を「その他の目標」へ移すのは不可。同じグループの `Clicks to call` の
扱いまで変わってしまうため、**個別アクションだけをサブにする**。

### 昇格後にやること

- 週次経営レビューで CPA を「HPB単独」ではなく「HPB＋LINE合算」で見る（分母が増えるのでCPAは下がる）
- `docs/ads-ops-guardrails.md` の判断基準を合算ベースへ更新

---

## 3. GA4の重複イベントを整理する（任意・優先度低）

`click_reserve` と `hpb_click` のように、同じ行動が2つの名前で二重に記録されている。
レポートを読むときに混乱するだけで実害は無いが、整理するなら：

- GA4 → 管理 → **イベントを作成** で、どの派生ルールが動いているかを確認する
- 派生名（`hpb_click` 等）に一本化するなら、元イベントはキーイベントから外す
- **サイト側のコード（`src/layouts/Base.astro`）は変えないこと。**
  Google広告のインポートが派生名に依存しているため、元イベントを止めると計測が止まる

---

## 4. AI検索（LLMO）の効果測定（施策E-5）

### GA4の「AI Assistant」チャネル

GA4のデフォルトチャネルグループに **AI Assistant** が既に存在し、実データも出ている。

直近28日：`copilot.com / ai-assistant` から **1セッション**。まだ最小だが、計測は動いている。

週次経営レビューに次の1行を追加する（Windsorで取得できる）：

```
connector: googleanalytics4
fields: ["default_channel_group", "session_source_medium", "sessions"]
date_preset: "last_7d"
→ default_channel_group == "AI Assistant" の行を見る
```

### Perplexityの捕捉（カスタムチャネルグループ）

PerplexityはGA4のデフォルト「AI Assistant」に含まれない。拾うには：

1. GA4 → 管理 → **チャネルグループ** → カスタムチャネルグループを作成（例：`AI検索`）
2. 条件：`セッションの参照元` が次のいずれかを含む
   `perplexity.ai` / `chatgpt.com` / `chat.openai.com` / `gemini.google.com` /
   `copilot.microsoft.com` / `copilot.com` / `claude.ai`
3. 探索レポートでこのチャネルグループを使う

> 注意：AI経由の流入は多くが `Direct` に混ざる（AIアプリ内ブラウザがリファラを落とすため）。
> チャネルで拾えるのは一部だけなので、**指名検索の伸び**（Search Consoleの「全力ストレッチ 岐阜」等）
> も併せて見ること。

### 週次経営レビューに追加する指標

- AI Assistant チャネルのセッション数（上記）
- 回数券1本あたり広告費（現状ベンチマーク：10,300円）
- CTA別の予約クリック内訳（下記）

---

## 5. CTA別の内訳を見る（2026-08-19に実装）

サイトの全CTAに設置場所ラベルを付けた。GA4では `cta_source`、Metaピクセルでは同名の
パラメータとして送られる。

| 値 | 場所 |
|---|---|
| `lp-fv` / `lp-pricing` / `lp-closing` / `lp-sticky` | 広告LP（/lp）の各CTA |
| `top` / `menu` / `access` / `staff` / `voice` / `reserve` | 各ページ下部の予約パネル |
| `column:<slug>` | コラム記事下部の予約パネル（記事別に分かる） |
| `symptom:<slug>` | 症状ページ下部の予約パネル |
| `header` / `header-drawer` / `footer` / `sticky-mobile` | 共通ナビ・固定バー |
| `inline` | 本文中に直書きされたリンク |

GA4の探索レポートで、ディメンションに `cta_source`（カスタムディメンションとして登録が必要）、
指標に `click_reserve` のイベント数を置くと、**どのページのどのCTAが予約を生んでいるか**が出る。

### GA4でのカスタムディメンション登録（未実施・要オーナー作業）

GA4 → 管理 → **カスタム定義** → カスタムディメンションを作成
- ディメンション名：`CTA設置場所`
- 範囲：イベント
- イベントパラメータ：`cta_source`

登録した時点以降のデータのみ集計される（遡及しない）ため、早めに入れておくとよい。

---

## 6. Metaピクセルの状態

`src/components/MetaPixel.astro` は環境変数 `PUBLIC_META_PIXEL_ID` が設定されているときだけ
出力される設計。**このセッションからは本番サイトへの到達が遮断されていて、
実際にピクセルが出ているかを確認できていない。**

オーナー確認事項：
- [ ] Vercel → プロジェクト → Settings → Environment Variables に
      `PUBLIC_META_PIXEL_ID` が本番環境向けに設定されているか
- [ ] Metaイベントマネージャで PageView / Schedule / Lead / Contact が届いているか
      （2026-08-19以降、予約クリック・電話タップでも **Lead** が届くようになっている）

### イベント設計（2026-08-19〜）

| CTA | 送るイベント |
|---|---|
| HPB予約・クーポン | `Schedule` ＋ `Lead` |
| LINE | `Lead` |
| 電話（tel:） | `Contact` ＋ `Lead` |

HPBは外部ドメインでサンクスページにピクセルを置けないため、サイト側で取れる
最も購入に近いシグナルが「予約クリック」になる。これをSchedule だけに入れると
Leadが LINE 分（28日で21件）しか溜まらず、キャンペーン目的をトラフィックから
**リード最適化**へ引き上げられない。両方送ることで、Schedule/Contact の内訳を
保ったまま Lead を集約できる（`content_name` で内訳を判別可能）。
GA4の実測（28日：予約273＋LINE21＋電話21＝315件）から、**Leadは週70件前後**になる見込み。

- 週50件を安定して超えたら、Meta広告のキャンペーン目的を **リード獲得** に変更する
- さらに回数券の成約実績は施策A（`docs/capi-offline-events.md`）でCAPI経由で送る
